


✅ 查重算法永远只有一个：findCrossDocumentSubstrings()
✅ TXT和PDF使用完全相同的查重逻辑
✅ 只是输出格式不同：TXT输出简化版，PDF输出带额外信息的版本
✅ 如果调整了查重算法，两种模式自动同步受益
✅ TXT和PDF使用相同的查重算法 findCrossDocumentSubstrings()
✅ 生成相同格式的 duplicate_report_*.txt
✅ TXT版本去掉：页码、置信度、bbox坐标、文字块索引
✅ TXT版本保留：基本信息、匹配段详情、字符位置、重复文本、统计汇总

detectDuplicatesFromText()

专门处理TXT文件的查重
✅ 使用和PDF完全相同的核心算法：BidChecker.findCrossDocumentSubstrings()
不包含bbox、页码等OCR特有信息
计算Jaccard相似度和增强相似度
saveTextDuplicateResult()

保存TXT查重结果（JSON + TXT报告）
格式与PDF的JSON结构相同，但textBlocks为空
saveSimplifiedTextReport()
